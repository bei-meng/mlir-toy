### 2026.9.14 #endif问题
-  error: Unexpected token at top level #endif
- 通过在endif后面加回车解决
- 如果使用了预编译头，VC会在所有CPP文件里寻找#include   "stdafx.h "，在找到这行代码之前，VC会忽略所有代码。



### 2026.9.15
#### 1. 问题：编译报错
- `assemblyFormat` 里的字面量必须用「反引号 `\``」包裹

#### 2. 使用trait:SameOperandsAndResultType时编译报错，td文件也加入了对应的include
- 只在TableGen（`.td` 文件）里引入了接口定义，但 C++ 编译阶段还需要头文件才能识别 `mlir::InferTypeOpInterface` 这个类。
- SameOperandsAndResultType` 内部自动继承了这个接口，一加这个 Trait 就触发了依赖。
- 所以必须在对应的cpp文件中加入#include "mlir/Interfaces/InferTypeOpInterface.h"
- 只有sub先报错,是因为加了:let hasVerifier = true;会自动生成完整的类型校验、不变量验证代码


### 2026.9.16
- ReturnOp 使用ReturnLike时，前面要加Terminator

- FuncOp的函数名符号的定义者`StringAttr`，要用SymbolNameAttr，存储自己本身的符号名，我就是这个符号
- CallOp函数调用者，符号的引用者`SymbolRefAttr`，存储对其他符号的引用，我要调用那个符号
- ReturnOp操作，返回值为可选参数时，要通过分组实现assemblyFormat，匹配没有返回值的情况
